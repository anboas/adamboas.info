---
import SiteLayout from '../../../layouts/SiteLayout.astro';
import SurfacePageHeader from '../../../components/SurfacePageHeader.astro';
import { withBase } from '../../../utils/withBase';
import researchScriptUrl from '../../../scripts/research-cgcyber-iom.js?url';

const assetBase = '/research/dashboard/';
const charts = [
	{
		id: 'cockpit',
		label: 'Opportunity Cockpit',
		src: withBase(`${assetBase}research-dashboard-cockpit.svg`),
		description:
			'Unified pursuit cockpit across NIWC PAC AA, CGCYBER IOM, and Naval Aviation Enterprise research blocks.',
		focusX: 0.5,
		focusY: 0.2,
	},
];

const tableSections = [
	{
		id: 'org',
		title: 'Next Actions / Research Improvements',
		description:
			'Concrete cross-block work queue: what to resolve, deepen, graph, monitor, and turn into capture action next.',
		countAttr: 'data-org-count',
		searchAttr: 'data-org-search',
		searchPlaceholder: 'Deep dives, CGCYBER, PMA-290, AA gap...',
		filterAttr: 'data-org-filter',
		filters: [
			['Domain', 'All blocks'],
			['Status', 'All statuses'],
			['IOM Relevance', 'All actions'],
			['Confidence', 'All priorities'],
		],
		tableAttr: 'data-org-table',
		bodyAttr: 'data-org-body',
		sortAttr: 'data-org-sort-key',
		headers: [
			['ID', 'ID'],
			['Node', 'Action'],
			['Role', 'Role'],
			['Status', 'Status'],
			['IOM Relevance', 'Summary'],
			['Code', 'Block'],
			['Domain', 'Domain'],
			['Type', 'Type'],
			['Evidence', 'Why it matters'],
			['Confidence', 'Priority'],
			['Source Refs', 'Refs'],
			['Source URL', 'Source'],
		],
	},
	{
		id: 'contracts',
		title: 'Unified Opportunity Dashboard',
		description: 'Comparable pursuit rows from the existing NIWC PAC AA, CGCYBER IOM, and NAE research blocks.',
		countAttr: 'data-contracts-count',
		searchAttr: 'data-contracts-search',
		searchPlaceholder: 'CGCYBER, Application Arsenal, TARCES, AIRWorks...',
		filterAttr: 'data-contracts-filter',
		filters: [
			['Prime', 'All buyers'],
			['Status', 'All statuses'],
			['IOM Seam / Relevance', 'All fit lanes'],
			['Confidence', 'All confidence'],
		],
		tableAttr: 'data-contracts-table',
		bodyAttr: 'data-contracts-body',
		sortAttr: 'data-contracts-sort-key',
		headers: [
			['ID', 'ID'],
			['Program', 'Pursuit'],
			['Prime', 'Buyer / owner'],
			['Status', 'Status'],
			['IOM Seam / Relevance', 'Fit lane'],
			['Award / Forecast ID', 'Vehicle / ID'],
			['Value / Term', 'Value / timing'],
			['Scope', 'Why it matters'],
			['Confidence', 'Confidence'],
			['Source Refs', 'Refs'],
			['Source URL', 'Block'],
		],
	},
	{
		id: 'competitors',
		title: 'Cross-Block Company / Teaming Map',
		description:
			'Companies that shape the current research blocks as incumbents, interface owners, likely threats, or possible partners.',
		countAttr: 'data-competitors-count',
		searchAttr: 'data-competitors-search',
		searchPlaceholder: 'GovCIO, Forward Slope, Synergy, BAE...',
		filterAttr: 'data-competitors-filter',
		filters: [
			['Tier', 'All posture'],
			['Vehicle', 'All vehicles'],
			['Confidence', 'All confidence'],
		],
		tableAttr: 'data-competitors-table',
		bodyAttr: 'data-competitors-body',
		sortAttr: 'data-competitors-sort-key',
		headers: [
			['ID', 'ID'],
			['Entity', 'Company / group'],
			['Tier', 'Posture'],
			['Recommended Posture', 'Recommended move'],
			['Power Base', 'Power base'],
			['Gap / Vulnerability', 'Gap / vulnerability'],
			['Vehicle', 'Vehicle / lane'],
			['Confidence', 'Confidence'],
			['Source Refs', 'Refs'],
			['Source URL', 'Block'],
		],
	},
	{
		id: 'sources',
		title: 'Source Register',
		description: 'Source anchors for the unified dashboard and the underlying research blocks.',
		countAttr: 'data-sources-count',
		searchAttr: 'data-sources-search',
		searchPlaceholder: 'NIWC, CGCYBER, NAE, System...',
		filterAttr: 'data-sources-filter',
		filters: [],
		tableAttr: 'data-sources-table',
		bodyAttr: 'data-sources-body',
		sortAttr: 'data-sources-sort-key',
		headers: [
			['ID', 'ID'],
			['Title', 'Title'],
			['Use', 'Use'],
			['URL', 'Source'],
		],
	},
];
---

<SiteLayout
	title="Research Dashboard"
	description="Private unified opportunity dashboard across NIWC PAC AA, CGCYBER IOM, and Naval Aviation Enterprise research."
	noindex={true}
	nofollow={true}
>
	<section
		class="research-app"
		data-cgcyber-research
		data-org-csv-url={withBase(`${assetBase}research-dashboard-actions.csv`)}
		data-contracts-csv-url={withBase(`${assetBase}research-dashboard-opportunities.csv`)}
		data-competitors-csv-url={withBase(`${assetBase}research-dashboard-companies.csv`)}
		data-sources-csv-url={withBase(`${assetBase}research-dashboard-source-register.csv`)}
	>
		<SurfacePageHeader
			stickyActive="none"
			eyebrowLinks={[{ label: 'Research', href: withBase('/research/') }, { label: 'Dashboard' }]}
			title="Research Dashboard"
			description="Unified capture-intel surface for the active research blocks: priority opportunities, company/teaming signals, research gaps, and next actions."
		>
			<div slot="actions" class="flex flex-wrap gap-2">
				<a class="ui-link-btn ui-link-btn-primary" href={withBase(`${assetBase}research-dashboard-opportunities.csv`)}
					>Opportunities CSV</a
				>
				<a class="ui-link-btn ui-link-btn-muted" href={withBase(`${assetBase}research-dashboard-companies.csv`)}
					>Companies CSV</a
				>
				<a class="ui-link-btn ui-link-btn-muted" href={withBase(`${assetBase}research-dashboard-actions.csv`)}
					>Actions CSV</a
				>
			</div>
		</SurfacePageHeader>

		<div class="research-banner mt-3 border border-teal-400/20 bg-teal-950/20 p-3 text-xs leading-5 text-slate-300">
			<span class="font-semibold text-teal-100">Portfolio view.</span>
			This does not replace the block atlases. It gives them one comparable operating surface for capture prioritization.
		</div>

		<nav class="research-jump mt-3" aria-label="Research section navigation">
			<a href={withBase('/research/')}>Research Index</a>
			<a href={withBase('/research/system/')}>System</a>
			<a href="#chart-title">Charts</a>
			<a href="#contracts-title">Opportunities</a>
			<a href="#competitors-title">Companies</a>
			<a href="#org-title">Next Actions</a>
			<a href="#sources-title">Sources</a>
			<a href={withBase('/research/niwc-pac/')}>NIWC PAC</a>
			<a href={withBase('/research/cgcyber-iom/')}>CGCYBER IOM</a>
			<a href={withBase('/research/naval-aviation-enterprise/')}>NAE</a>
		</nav>

		<section class="mt-4 grid gap-3 md:grid-cols-2 xl:grid-cols-5" aria-label="Research summary">
			<div class="metric-panel">
				<b>3</b>
				<span>Active pursuit blocks</span>
			</div>
			<div class="metric-panel">
				<b>12</b>
				<span>Priority opportunities</span>
			</div>
			<div class="metric-panel">
				<b>10</b>
				<span>Company signals</span>
			</div>
			<div class="metric-panel">
				<b>8</b>
				<span>Next actions</span>
			</div>
			<div class="metric-panel">
				<b>1</b>
				<span>Unified cockpit</span>
			</div>
		</section>

		<section class="mt-4 border border-slate-800 bg-slate-950/95" aria-labelledby="chart-title">
			<div class="flex flex-col gap-3 border-b border-slate-800 p-3 lg:flex-row lg:items-start lg:justify-between">
				<div class="min-w-0">
					<h2 id="chart-title" class="text-base font-semibold text-slate-50">Dashboard Chart</h2>
					<p class="mt-1 max-w-5xl text-xs leading-5 text-slate-400" data-chart-description>
						{charts[0].description}
					</p>
				</div>
				<div class="flex flex-wrap gap-2 text-xs" aria-label="Chart view controls">
					{
						charts.map((chart, index) => (
							<button
								class:list={['chart-tab', index === 0 && 'chart-tab-active']}
								type="button"
								data-chart-tab={chart.id}
								data-chart-src={chart.src}
								data-chart-tab-description={chart.description}
								data-chart-focus-x={chart.focusX}
								data-chart-focus-y={chart.focusY}
							>
								{chart.label}
							</button>
						))
					}
				</div>
			</div>

			<div class="chart-toolbar border-b border-slate-800 p-3">
				<div class="flex flex-wrap items-center gap-2">
					<button type="button" class="control-btn" data-chart-action="zoom-out">Zoom -</button>
					<button type="button" class="control-btn" data-chart-action="zoom-in">Zoom +</button>
					<button type="button" class="control-btn" data-chart-action="fit">Fit</button>
					<button type="button" class="control-btn" data-chart-action="reset">100%</button>
					<a class="control-btn no-underline" data-chart-download href={charts[0].src}>Open SVG</a>
					<span class="ui-pill" data-chart-zoom>100%</span>
				</div>
			</div>

			<div class="chart-stage" data-chart-stage>
				<img
					src={charts[0].src}
					alt="Unified research opportunity dashboard chart"
					class="chart-image"
					data-chart-image
					draggable="false"
				/>
			</div>
		</section>

		{
			tableSections.map((section) => (
				<section class="mt-4 border border-slate-800 bg-slate-950/95" aria-labelledby={`${section.id}-title`}>
					<div class="border-b border-slate-800 p-3">
						<div class="flex flex-col gap-3 xl:flex-row xl:items-start xl:justify-between">
							<div class="min-w-0">
								<h2 id={`${section.id}-title`} class="text-base font-semibold text-slate-50">
									{section.title}
								</h2>
								<p class="mt-1 max-w-4xl text-xs leading-5 text-slate-400">{section.description}</p>
							</div>
							<div class="text-xs text-slate-400">
								<span {...{ [section.countAttr]: true }}>Loading...</span>
							</div>
						</div>

						<div class="mt-3 grid gap-2 md:grid-cols-2 xl:grid-cols-5">
							<label class="filter-field md:col-span-2 xl:col-span-1">
								<span>Search</span>
								<input type="search" {...{ [section.searchAttr]: true }} placeholder={section.searchPlaceholder} />
							</label>
							{section.filters.map(([key, label]) => (
								<label class="filter-field">
									<span>{key}</span>
									<select {...{ [section.filterAttr]: key }}>
										<option value="">{label}</option>
									</select>
								</label>
							))}
						</div>
					</div>

					<div class:list={['roster-table-wrap', `${section.id}-table-wrap`]}>
						<table class="roster-table" {...{ [section.tableAttr]: true }}>
							<thead>
								<tr>
									{section.headers.map(([key, label]) => (
										<th>
											<button type="button" {...{ [section.sortAttr]: key }}>
												{label}
											</button>
										</th>
									))}
								</tr>
							</thead>
							<tbody {...{ [section.bodyAttr]: true }}>
								<tr>
									<td colspan={section.headers.length} class="px-3 py-6 text-center text-sm text-slate-400">
										Loading...
									</td>
								</tr>
							</tbody>
						</table>
					</div>
				</section>
			))
		}
	</section>

	<script type="module" src={researchScriptUrl}></script>
</SiteLayout>

<style>
	.research-app {
		--research-border: rgba(51, 65, 85, 0.92);
		--research-muted: rgb(148 163 184);
	}

	.research-banner,
	.metric-panel,
	.chart-tab,
	.control-btn,
	.filter-field input,
	.filter-field select {
		border-radius: 0;
	}

	.research-jump {
		display: flex;
		flex-wrap: wrap;
		gap: 0.5rem;
		border: 1px solid rgba(51, 65, 85, 0.92);
		background: rgba(2, 6, 23, 0.86);
		padding: 0.65rem;
	}

	.research-jump a {
		border: 1px solid rgba(71, 85, 105, 0.88);
		padding: 0.35rem 0.65rem;
		color: rgb(203 213 225);
		font-size: 0.75rem;
		line-height: 1.2;
		text-decoration: none;
	}

	.research-jump a:hover,
	.research-jump a:focus-visible {
		border-color: rgba(45, 212, 191, 0.65);
		color: rgb(240 253 250);
	}

	.metric-panel {
		border: 1px solid var(--research-border);
		background: rgb(2 6 23);
		padding: 0.85rem;
	}

	.metric-panel b {
		display: block;
		font-size: 1.42rem;
		line-height: 1.1;
		color: rgb(248 250 252);
	}

	.metric-panel span {
		margin-top: 0.25rem;
		display: block;
		font-size: 0.7rem;
		line-height: 1.25;
		color: var(--research-muted);
		text-transform: uppercase;
	}

	.chart-tab,
	.control-btn {
		min-height: 2rem;
		border: 1px solid rgba(71, 85, 105, 0.88);
		background: rgba(2, 6, 23, 0.88);
		padding: 0.35rem 0.65rem;
		color: rgb(203 213 225);
		line-height: 1.2;
	}

	.chart-tab:hover,
	.control-btn:hover {
		border-color: rgba(45, 212, 191, 0.65);
		color: rgb(240 253 250);
	}

	.chart-tab-active {
		border-color: rgba(45, 212, 191, 0.78);
		background: rgba(20, 184, 166, 0.2);
		color: rgb(240 253 250);
	}

	.chart-stage {
		position: relative;
		height: min(68vh, 680px);
		min-height: 390px;
		overflow: hidden;
		background: rgb(248 250 252);
		cursor: grab;
		touch-action: none;
	}

	.chart-stage:active {
		cursor: grabbing;
	}

	.chart-image {
		display: block;
		max-width: none;
		transform-origin: 0 0;
		user-select: none;
		will-change: transform;
	}

	.filter-field {
		display: flex;
		min-width: 0;
		flex-direction: column;
		gap: 0.35rem;
	}

	.filter-field span {
		font-size: 0.68rem;
		line-height: 1.2;
		color: var(--research-muted);
		text-transform: uppercase;
	}

	.filter-field input,
	.filter-field select {
		width: 100%;
		min-height: 2.35rem;
		border: 1px solid rgba(71, 85, 105, 0.82);
		background: rgba(2, 6, 23, 0.86);
		padding: 0.45rem 0.6rem;
		color: rgb(226 232 240);
		font-size: 0.82rem;
		line-height: 1.25;
	}

	.roster-table-wrap {
		max-height: 72vh;
		overflow: auto;
		background: rgb(2 6 23);
		scrollbar-color: rgba(45, 212, 191, 0.48) rgba(15, 23, 42, 0.92);
	}

	.roster-table {
		--id-col-width: 4.25rem;
		--name-col-width: 18rem;
		width: 100%;
		min-width: 1480px;
		border-collapse: separate;
		border-spacing: 0;
		font-size: 0.78rem;
		line-height: 1.42;
	}

	.sources-table-wrap .roster-table {
		min-width: 980px;
	}

	.roster-table th,
	.research-app :global(.roster-table td) {
		border-bottom: 1px solid rgba(30, 41, 59, 0.95);
		padding: 0.62rem 0.7rem;
		text-align: left;
		vertical-align: top;
	}

	.roster-table th {
		position: sticky;
		top: 0;
		z-index: 4;
		border-bottom-color: rgba(45, 212, 191, 0.28);
		background: rgb(15 23 42);
		box-shadow: inset 0 -1px 0 rgba(45, 212, 191, 0.24);
	}

	.roster-table th button {
		display: inline-flex;
		align-items: center;
		gap: 0.25rem;
		border: 0;
		background: transparent;
		padding: 0;
		color: rgb(226 232 240);
		font: inherit;
		font-weight: 700;
	}

	.roster-table th button::after {
		content: '↕';
		color: rgb(100 116 139);
		font-size: 0.68rem;
	}

	.roster-table th button[data-sort-state='asc']::after {
		content: '↑';
		color: rgb(45 212 191);
	}

	.roster-table th button[data-sort-state='desc']::after {
		content: '↓';
		color: rgb(45 212 191);
	}

	.roster-table th:nth-child(1),
	.research-app :global(.roster-table td:nth-child(1)) {
		position: sticky;
		left: 0;
		z-index: 3;
		width: var(--id-col-width);
		min-width: var(--id-col-width);
	}

	.roster-table th:nth-child(2),
	.research-app :global(.roster-table td:nth-child(2)) {
		position: sticky;
		left: var(--id-col-width);
		z-index: 3;
		width: var(--name-col-width);
		min-width: var(--name-col-width);
		box-shadow: 1px 0 0 rgba(45, 212, 191, 0.18);
	}

	.roster-table th:nth-child(1),
	.roster-table th:nth-child(2) {
		z-index: 5;
	}

	.research-app :global(.roster-row) {
		background: rgb(2 6 23);
		box-shadow: inset 3px 0 0 rgba(71, 85, 105, 0.72);
	}

	.research-app :global(.roster-row:nth-child(even)) {
		background: rgb(8 13 27);
	}

	.research-app :global(.roster-row:hover),
	.research-app :global(.roster-row-selected) {
		background: rgb(12 24 40);
		box-shadow: inset 3px 0 0 rgba(45, 212, 191, 0.95);
	}

	.research-app :global(.roster-row td:nth-child(1)),
	.research-app :global(.roster-row td:nth-child(2)) {
		background: inherit;
	}

	.research-app :global(.cell-name strong) {
		color: rgb(248 250 252);
	}

	.research-app :global(.status-badge) {
		display: inline-flex;
		max-width: 100%;
		border: 1px solid rgba(148, 163, 184, 0.32);
		padding: 0.18rem 0.4rem;
		background: rgba(15, 23, 42, 0.92);
		color: rgb(226 232 240);
		font-size: 0.7rem;
		font-weight: 700;
		line-height: 1.2;
	}

	.research-app :global(.status-current),
	.research-app :global(.status-official) {
		border-color: rgba(45, 212, 191, 0.48);
		color: rgb(204 251 241);
	}

	.research-app :global(.status-gap),
	.research-app :global(.status-forecast) {
		border-color: rgba(245, 158, 11, 0.5);
		color: rgb(253 230 138);
	}

	.research-app :global(.source-link) {
		color: rgb(94 234 212);
		text-decoration: none;
	}

	.research-app :global(.source-link:hover) {
		text-decoration: underline;
	}

	@media (max-width: 720px) {
		.chart-stage {
			height: 58vh;
			min-height: 360px;
		}

		.roster-table {
			--name-col-width: 14rem;
			min-width: 1320px;
		}
	}
</style>
